/** Auto-generated catalog module (intentionally large for testing). */
export const CATALOG_VERSION = 4;

export function catalogRule1(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule2(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule3(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule4(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule5(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule6(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule7(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule8(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule9(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule10(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule11(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule12(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule13(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule14(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule15(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule16(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule17(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule18(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule19(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule20(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule21(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule22(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule23(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule24(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule25(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule26(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule27(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule28(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule29(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule30(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule31(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule32(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule33(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule34(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule35(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule36(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule37(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule38(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule39(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule40(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule41(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule42(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule43(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule44(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule45(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule46(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule47(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule48(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule49(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule50(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule51(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule52(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule53(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule54(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule55(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule56(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule57(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule58(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule59(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule60(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule61(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule62(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule63(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule64(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule65(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule66(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule67(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule68(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule69(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule70(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule71(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule72(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule73(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule74(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule75(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule76(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule77(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule78(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule79(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule80(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule81(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule82(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule83(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule84(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule85(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule86(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule87(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule88(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule89(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule90(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule91(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule92(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule93(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule94(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule95(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule96(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule97(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule98(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule99(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule100(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule101(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule102(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule103(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule104(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule105(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule106(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule107(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule108(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule109(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule110(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule111(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule112(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule113(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule114(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule115(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule116(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule117(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule118(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule119(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule120(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule121(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule122(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule123(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule124(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule125(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule126(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule127(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule128(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule129(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule130(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule131(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule132(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule133(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule134(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule135(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule136(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule137(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule138(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule139(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule140(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule141(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule142(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule143(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule144(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule145(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule146(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule147(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule148(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule149(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule150(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule151(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule152(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule153(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule154(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule155(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule156(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule157(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule158(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule159(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule160(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule161(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule162(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule163(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule164(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule165(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule166(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule167(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule168(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule169(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule170(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule171(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule172(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule173(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule174(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule175(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule176(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule177(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule178(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule179(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule180(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule181(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule182(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule183(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule184(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule185(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule186(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule187(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule188(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule189(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule190(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule191(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule192(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule193(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule194(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule195(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule196(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule197(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule198(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule199(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule200(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule201(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule202(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule203(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule204(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule205(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule206(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule207(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule208(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule209(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule210(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule211(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule212(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule213(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule214(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule215(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule216(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule217(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule218(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule219(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule220(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule221(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule222(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule223(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule224(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule225(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule226(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule227(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule228(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule229(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule230(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule231(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule232(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule233(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule234(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule235(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule236(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule237(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule238(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule239(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule240(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule241(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule242(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule243(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule244(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule245(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule246(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule247(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule248(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule249(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule250(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule251(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule252(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule253(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule254(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule255(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule256(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule257(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule258(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule259(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule260(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule261(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule262(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule263(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule264(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule265(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule266(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule267(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule268(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule269(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule270(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule271(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule272(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule273(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule274(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule275(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule276(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule277(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule278(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule279(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule280(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule281(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule282(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule283(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule284(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule285(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule286(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule287(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule288(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule289(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule290(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule291(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule292(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule293(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule294(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule295(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule296(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule297(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule298(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule299(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule300(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule301(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule302(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule303(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule304(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule305(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule306(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule307(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule308(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule309(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule310(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule311(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule312(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule313(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule314(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule315(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule316(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule317(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule318(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule319(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule320(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule321(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule322(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule323(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule324(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule325(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule326(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule327(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule328(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule329(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule330(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule331(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule332(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule333(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule334(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule335(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule336(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule337(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule338(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule339(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule340(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule341(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule342(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule343(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule344(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule345(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule346(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule347(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule348(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule349(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule350(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule351(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule352(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule353(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule354(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule355(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule356(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule357(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule358(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule359(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule360(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule361(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule362(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule363(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule364(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule365(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule366(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule367(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule368(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule369(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule370(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule371(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule372(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule373(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule374(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule375(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule376(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule377(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule378(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule379(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule380(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule381(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule382(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule383(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule384(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule385(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule386(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule387(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule388(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule389(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule390(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule391(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule392(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule393(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule394(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule395(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule396(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule397(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule398(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule399(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule400(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule401(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule402(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule403(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule404(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule405(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule406(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule407(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule408(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule409(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule410(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule411(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule412(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule413(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule414(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule415(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule416(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule417(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule418(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule419(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule420(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule421(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule422(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule423(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule424(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule425(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule426(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule427(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule428(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule429(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule430(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule431(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule432(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule433(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule434(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule435(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule436(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule437(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule438(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule439(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule440(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule441(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule442(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule443(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule444(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule445(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule446(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule447(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule448(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule449(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule450(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule451(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule452(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule453(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule454(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule455(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule456(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule457(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule458(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule459(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule460(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule461(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule462(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule463(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule464(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule465(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule466(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule467(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule468(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule469(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule470(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule471(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule472(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule473(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule474(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule475(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule476(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule477(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule478(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule479(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule480(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule481(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule482(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule483(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule484(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function catalogRule485(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_10' && item.active === true;
}

export function catalogRule486(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_11' && item.active === true;
}

export function catalogRule487(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_12' && item.active === true;
}

export function catalogRule488(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_13' && item.active === true;
}

export function catalogRule489(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_14' && item.active === true;
}

export function catalogRule490(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_15' && item.active === true;
}

export function catalogRule491(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_16' && item.active === true;
}

export function catalogRule492(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_17' && item.active === true;
}

export function catalogRule493(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_18' && item.active === true;
}

export function catalogRule494(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_19' && item.active === true;
}

export function catalogRule495(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_20' && item.active === true;
}

export function catalogRule496(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_21' && item.active === true;
}

export function catalogRule497(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_22' && item.active === true;
}

export function catalogRule498(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_23' && item.active === true;
}

export function catalogRule499(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_24' && item.active === true;
}

export function catalogRule500(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_0' && item.active === true;
}

export function catalogRule501(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_1' && item.active === true;
}

export function catalogRule502(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_2' && item.active === true;
}

export function catalogRule503(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_3' && item.active === true;
}

export function catalogRule504(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_4' && item.active === true;
}

export function catalogRule505(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_5' && item.active === true;
}

export function catalogRule506(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_6' && item.active === true;
}

export function catalogRule507(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_7' && item.active === true;
}

export function catalogRule508(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_8' && item.active === true;
}

export function catalogRule509(item: { category?: string; active?: boolean }): boolean {
  return item.category === 'cat_9' && item.active === true;
}

export function summarizeCatalog(items: Array<{ active?: boolean }>): number {
  return items.filter((item) => item.active).length;
}
